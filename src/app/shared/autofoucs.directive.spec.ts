import { AutofoucsDirective } from './autofoucs.directive';

describe('AutofoucsDirective', () => {
  it('should create an instance', () => {
    const directive = new AutofoucsDirective();
    expect(directive).toBeTruthy();
  });
});
